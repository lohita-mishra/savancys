dependencies {
	compile project(":modules:savansys:savansys-api")

	compileOnly group: "com.liferay.portal", name: "release.dxp.api"
}

buildService {
	apiDir = "../savansys-api/src/main/java"
}

group = "com.savancys"